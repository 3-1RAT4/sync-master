# Read-only role - see docs/web-ui.md for how it's provisioned.
DATABASE_URL="postgresql://sync_master_web:CHANGEME@localhost:5432/mydb"
PORT=4000
# 127.0.0.1 keeps it on this machine. 0.0.0.0 exposes it to the whole network
# with no authentication - only on a network you trust.
HOST=127.0.0.1
